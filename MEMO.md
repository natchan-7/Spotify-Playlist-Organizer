# MEMO

このファイルは、実装候補や判断メモを追記しやすく保つための作業メモです。

## 使い方

- 新しい案はまず `候補` に追加する
- 着手したら `進行中` に移す
- 終わったら `完了` に移す
- 詳細メモが必要なら、項目の直下に1〜3行だけ追記する
- 迷ったことは `未決定` に残す

## 現在できていること

- Spotify ログイン
- プレイリスト一覧取得
- プレイリストの楽曲取得
- アーティストジャンル取得
- 自動タグ生成
- 手動タグ追加・削除
- タグ付き楽曲から新規プレイリスト作成
- ブラウザ保存データの確認・削除
- 選択中プレイリストのジャンル集計・主要アーティスト表示
- 楽曲一覧のアーティスト名から Spotify へのリンク表示
- 楽曲一覧の曲名・アーティスト名による検索・フィルター
- 手動タグの JSON エクスポート / インポート
- 楽曲一覧のアーティスト選択による絞り込み
- 楽曲一覧の人気度バッジ表示・人気度による並び替え
- アーティスト単位でのプレイリスト作成
- 楽曲の複数選択による一括タグ付け
- アーティスト詳細のポップオーバー表示（ジャンル・人気度・フォロワー数）
- 手動タグ入力欄のグループ表示・空状態表示・入力チェック

## 候補

### 優先度高

- なし

### 優先度中

- なし

### 優先度低

- [ ] タグ管理ビュー（全タグの一覧・リネーム・統合・削除）を追加する
- [ ] アーティスト URL / ID 手入力検索を追加する
- [ ] プレイリスト傾向レポートを追加する
- [ ] 既存 UI 全体の情報量と配置を見直す

## 進行中

- なし

## 完了

- [x] プレイリスト取得・詳細取得まわりを最新の `items` エンドポイント前提に整理
- [x] Spotify API ガイドラインに合わせて PKCE + refresh token, 429 backoff, redirect URI 検証を整理
- [x] Spotify 由来のアーティストキャッシュと自動タグの長期保存をやめ、手動タグのみ保存する形に整理
- [x] ジャンル集計カードを追加する
- [x] 主要アーティスト一覧を表示する（Spotify リンク付き）
- [x] 未使用のプレースホルダー関数（`persistGeneratedAutoTags` / `getStoredArtistGenreCache` / `saveArtistGenreCache`）を削除
- [x] React Error Boundary を追加
- [x] GitHub Actions でビルド確認 CI を追加
- [x] App.jsx の純粋関数ヘルパーを `utils/appHelpers.js` / `utils/formatting.js` へ分割
- [x] Spotify 側の制限で失敗したときの文言を、利用者向けに簡潔にする
- [x] アーティスト名を Spotify リンク化する（楽曲一覧側）
- [x] Spotify のジャンル情報だけに頼らない補助ルールを増やす
  - 曲名・アルバム名から live / remix / acoustic / instrumental / karaoke / cover / demo / remastered / soundtrack / extended / unplugged をパターン検出し、ジャンルタグや既存のアーティスト名フォールバックに追加する形で自動タグ化
- [x] 楽曲一覧に検索・フィルターを追加する（曲名・アーティスト名でインクリメンタル検索）
  - 取得済みの `tracks` に対するクライアント側フィルターのみで実装。一致件数表示・クリアボタン・該当なし時の通知も追加
- [x] プレイリスト作成時にタグ選択でマッチ件数をプレビュー表示する
  - 選択後の「一致した曲数」「プレビュー一覧」は Step 8 で既に実装済みだったため、未実装だったタグ候補一覧（ドロップダウン）側に各タグごとのマッチ曲数を表示するよう追加
- [x] 手動タグのエクスポート / インポート（JSON）を追加する
  - ブラウザデータ管理パネルに「エクスポート」「インポート」ボタンを追加。エクスポートは `trackTags` を `{version, trackTags}` 形式の JSON ファイルとしてダウンロードし、インポートは選択した JSON を既存の保存データへマージ（同一曲のタグは重複排除して結合）し、追加件数を通知する
- [x] アーティストで曲を絞り込む
  - 楽曲一覧の検索欄の横に、プレイリスト内のアーティスト名を選べるドロップダウンを追加。検索キーワードと AND 条件で絞り込み、表示件数バッジ・該当なし通知も既存の検索フィルターと共通化
- [x] 人気度ベースの並び替えを追加する
  - 楽曲一覧の絞り込み行に並び順セレクトを追加（標準 / 人気度が高い順 / 低い順）。`normalizeTrackItem` で `popularity` を取得し、各曲のタイトル行に簡易な数値バッジで表示
- [x] アーティスト単位でプレイリストを作成する
  - 既存のプレイリスト作成パネルの「条件の種類」にアーティストを追加。アーティスト名（大文字小文字無視）でマッチする曲を集計・絞り込み、新規プレイリスト作成・成功メッセージもアーティスト向けの文言に対応
- [x] 複数曲への一括タグ付け（チェックボックス選択 + バルク操作）を追加する
  - 楽曲一覧の各行にチェックボックスを追加し、「表示中の曲をすべて選択」と組み合わせて複数選択できるようにした。選択中は一括タグ入力欄が表示され、選択した全曲へ既存の `onAddUserTag` を順番に適用し、追加件数・重複件数を通知する
- [x] アーティスト詳細をポップオーバーで表示する
  - アーティスト名の横に詳細表示ボタン（ⓘ）を追加し、クリックすると `GET /artists/{id}` を呼び出してジャンル・人気度・フォロワー数・画像・Spotify リンクをポップオーバー表示する。取得結果はその場限りで保持し、長期保存はしない
- [x] 手動タグ入力やタグ表示まわりの UI を見直す
  - タグ一覧に「自動」「手動」のラベルを追加してグループを区別しやすくし、タグが無い曲には「タグはまだありません」と表示するようにした。手動タグ追加ボタン（曲ごと・一括タグ付け両方）は入力が空のとき無効化するようにした

## 実装メモ

### アーティスト情報の使い道

- `genres`: 自動タグ、集計、フィルターに使える
- `id`: キャッシュキー、再取得、絞り込み条件に使える
- `name`: 表示、検索、補助タグに使える
- `images`: アーティスト詳細 UI に使える
- `external_urls.spotify`: Spotify への外部リンクに使える
- `popularity`: 並び替えや分析表示に使える
- `followers.total`: 補助情報として表示できる

### API まわり

- プレイリスト内アイテム取得は `GET /playlists/{id}/items` を基準にする
- アーティスト情報取得は基本的に `GET /artists?ids=...` の一括取得を使う
- 単体取得 `GET /artists/{id}` は手入力検索や詳細画面で使う余地がある
- `genres` が空のアーティストもあるので、空配列を前提に扱う
- OpenAPI schema をエンドポイントの正本として扱う
- `429` は `Retry-After` を優先し、なければ指数バックオフする

### UI まわり

- 小さく始めるなら「リンク化」「集計表示」「一覧表示」から入る
- クリック操作を増やす場合は、曲一覧の可読性を落とさないようにする
- 新しい情報を足すときは、まず折りたたみか補助カードで検証する

### 準拠ルール

- 認証は Authorization Code with PKCE を使う
- redirect URI は HTTPS を使い、ローカル開発時のみ `http://127.0.0.1` を使う
- `http://localhost` は使わない
- Client Secret はフロントエンドに置かない
- Spotify 由来データは長期キャッシュしない

## 未決定

- なし（アーティスト詳細表示・アーティスト単位プレイリスト作成・人気度の出し方は、優先度中の各項目に決定事項として記載済み）

## 追記テンプレート

```md
### YYYY-MM-DD
- 追加したいこと:
- 目的:
- 使うデータ:
- 実装メモ:
```
