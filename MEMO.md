# MEMO

このファイルは、実装候補や判断メモを追記しやすく保つための作業メモです。

## 使い方

- 新しい案はまず `候補` に追加する
- 着手したら `進行中` に移す
- 終わったら `完了` に移す
- 詳細メモが必要なら、項目の直下に1〜3行だけ追記する
- 迷ったことは `未決定` に残す

## 現在できていること

- Spotify ログイン
- プレイリスト一覧取得
- プレイリストの楽曲取得
- アーティストジャンル取得
- 自動タグ生成
- 手動タグ追加・削除
- タグ付き楽曲から新規プレイリスト作成
- ブラウザ保存データの確認・削除

## 候補

### 優先度高

- [x] Spotify 側の制限で失敗したときの文言を、利用者向けに簡潔にする
- [x] Spotify のジャンル情報だけに頼らない補助ルールを増やす
- [x] アーティスト名を Spotify リンク化する
- [x] ジャンル集計カードを追加する
- [x] 主要アーティスト一覧を表示する

#### 高優先度の細分化

##### すぐ実装できるもの

- [ ] エラー文言マップを整理する
  - `401 / 403 / 429 / その他` の表示方針を `App.jsx` で揃える
  - 「Spotify 側の制限」と「アプリ内部の失敗」を文言で分ける
- [ ] 楽曲の正規化データにアーティスト Spotify URL を含める
  - `spotifyApi.js` の normalized track shape を壊さず拡張する
  - `artists[].spotifyUrl` を UI で使えるようにする
- [ ] 楽曲一覧のアーティスト名をリンク化する
  - URL があるアーティストだけリンク表示にする
  - 既存の楽曲一覧の可読性を落とさない
- [ ] アーティスト出現回数の集計 helper を追加する
  - `tracks` からアーティスト単位の件数を集計する
  - 上位一覧表示にそのまま使える形にする
- [ ] 主要アーティスト一覧カードを追加する
  - 名前 / 出現曲数 / Spotify リンクを表示する
  - まずは補助カードとして小さく始める
- [ ] 自動タグ頻度の集計 helper を追加する
  - `tracks.autoTags` からタグ頻度を数える
  - 表示件数を上位数件に絞る
- [ ] ジャンル集計カードを追加する
  - 上位タグをカード表示する
  - fallback タグを含めるかは実装時に明記する

##### 仕様を詰めながら実装するもの

- [ ] 自動タグ生成ロジックを helper 単位に分ける
  - genre 由来タグと fallback タグの境界を分かりやすくする
- [ ] 補助ルールの追加方針を 3 段階で整理する
  - 第1段階: Spotify genre をそのまま使う
  - 第2段階: genre が空ならアーティスト名を使う
  - 第3段階: genre 文字列の分解や正規化を追加する
- [ ] 補助ルール用の除外条件を増やす
  - 汎用語、短すぎる語、ノイズになりやすい語を外す
- [ ] 補助タグを集計表示に含めるか決める
  - 「ジャンル集計」と「補助タグ集計」を分ける余地を残す
- [ ] README の補足文言を実装に合わせて更新する
  - 自動タグ生成ルールの説明を現在の実装と揃える

#### 実装順メモ

- [ ] Task A: エラー文言マップ作成
- [ ] Task B: normalized track に artist `spotifyUrl` を追加
- [ ] Task C: 楽曲一覧のアーティストリンク UI
- [ ] Task D: アーティスト出現回数集計 helper
- [ ] Task E: 主要アーティストカード UI
- [ ] Task F: 自動タグ頻度集計 helper
- [ ] Task G: ジャンル集計カード UI
- [ ] Task H: auto tag 生成ロジックの分離
- [ ] Task I: 補助ルール追加
- [ ] Task J: README / MEMO 更新

#### 最小着手セット

- [ ] `Task A + B + C + D + E + F + G` を先に進める
- [ ] `Task H + I + J` は仕様確認を挟みつつ進める

### 優先度中

- [ ] アーティスト詳細ポップオーバーを表示する
- [ ] アーティストで曲を絞り込む
- [ ] アーティスト単位でプレイリストを作成する
- [ ] 人気度ベースの並び替えを追加する
- [ ] 手動タグ入力やタグ表示まわりの UI を見直す

### 優先度低

- [ ] アーティスト URL / ID 手入力検索を追加する
- [ ] プレイリスト傾向レポートを追加する
- [ ] 既存 UI 全体の情報量と配置を見直す

## 進行中

- なし

## 完了

- [x] プレイリスト取得・詳細取得まわりを最新の `items` エンドポイント前提に整理
- [x] Spotify API ガイドラインに合わせて PKCE + refresh token, 429 backoff, redirect URI 検証を整理
- [x] Spotify 由来のアーティストキャッシュと自動タグの長期保存をやめ、手動タグのみ保存する形に整理

## 実装メモ

### アーティスト情報の使い道

- `genres`: 自動タグ、集計、フィルターに使える
- `id`: キャッシュキー、再取得、絞り込み条件に使える
- `name`: 表示、検索、補助タグに使える
- `images`: アーティスト詳細 UI に使える
- `external_urls.spotify`: Spotify への外部リンクに使える
- `popularity`: 並び替えや分析表示に使える
- `followers.total`: 補助情報として表示できる

### API まわり

- プレイリスト内アイテム取得は `GET /playlists/{id}/items` を基準にする
- アーティスト情報取得は基本的に `GET /artists?ids=...` の一括取得を使う
- 単体取得 `GET /artists/{id}` は手入力検索や詳細画面で使う余地がある
- `genres` が空のアーティストもあるので、空配列を前提に扱う
- OpenAPI schema をエンドポイントの正本として扱う
- `429` は `Retry-After` を優先し、なければ指数バックオフする

### UI まわり

- 小さく始めるなら「リンク化」「集計表示」「一覧表示」から入る
- クリック操作を増やす場合は、曲一覧の可読性を落とさないようにする
- 新しい情報を足すときは、まず折りたたみか補助カードで検証する

### 準拠ルール

- 認証は Authorization Code with PKCE を使う
- redirect URI は HTTPS を使い、ローカル開発時のみ `http://127.0.0.1` を使う
- `http://localhost` は使わない
- Client Secret はフロントエンドに置かない
- Spotify 由来データは長期キャッシュしない

## 未決定

- アーティスト詳細はポップオーバーにするか、右パネルにするか
- アーティスト単位プレイリスト作成を既存タグ作成 UI に統合するか、別 UI にするか
- 人気度を曲一覧に直接出すか、分析表示だけに使うか

## 追記テンプレート

```md
### YYYY-MM-DD
- 追加したいこと:
- 目的:
- 使うデータ:
- 実装メモ:
```
